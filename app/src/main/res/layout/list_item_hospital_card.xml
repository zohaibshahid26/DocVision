<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_margin="8dp"
    android:background="@drawable/rounded_border"

    android:paddingVertical="40dp"
    android:paddingHorizontal="80dp"

    android:layout_gravity="center"
    android:gravity="center"> <!-- Center content within the LinearLayout -->

    <!-- Image at the top -->
    <ImageView
        android:id="@+id/item_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:tint="@android:color/white"
        android:padding="8dp"
        android:background="@drawable/circle_background_color_red"
        android:scaleType="centerCrop"
        android:src="@drawable/hospital"/> <!-- Replace with your drawable resource -->

    <!-- Hospital Name -->
    <TextView
        android:id="@+id/item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_marginTop="8dp"
        android:text="Hospital Name"
        android:gravity="center"/> <!-- Center the text -->

    <!-- Number of Available Doctors -->
    <TextView
        android:id="@+id/item_doctors"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:layout_marginTop="4dp"
        android:text="Doctors: 10"
        android:gravity="center"/> <!-- Center the text -->

    <!-- Location -->
    <TextView
        android:id="@+id/item_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="@android:color/black"
        android:layout_marginTop="4dp"
        android:text="Location"
        android:gravity="center"/> <!-- Center the text -->
</LinearLayout>
